package discover;

/**
 * There is a library for rsync in Java yajsync. Looks interesting.
 *
 * So, the idea is that a central object will scan the network, ssh into the machine,
 * apply rsync and copy the target machine in it's entirety to a local disk. Then index the
 * local disk and make it available to be searched.
 */
public class Finder {

    public void backup() {

    }

}
